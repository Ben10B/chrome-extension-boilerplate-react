export interface ProductTab {
  id: number;
  title: string;
  url: string;
  favIconUrl: string;
}

export interface ProductPage {
  image: string;
  title: string;
  price: string;
  details: string;
}
